name: japan_maps
description: "A new Flutter package project."
version: 1.0.0
repository: https://github.com/ironmax123/japan_maps
homepage:

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  assets:
    - assets/map.geojson
